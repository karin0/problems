#include <cstdio>
#include <cmath>
#include <sys/time.h>

typedef long long ll;
using std::sqrt;

int main() {
    static int t, ks;
    scanf("%d", &t);
    for (ks = 0; ks < t; ++ks) {
        printf("%lf\n", sqrt(13));

    }
    return 0;
}
